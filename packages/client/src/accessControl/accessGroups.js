import { accessGroups as ComponentAccessGroups } from '@/components';

const LG = console.log; // eslint-disable-line no-console, no-unused-vars

export default Object.assign(
  {},
  ComponentAccessGroups,
);
