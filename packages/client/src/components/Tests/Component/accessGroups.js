const RESOURCE_NAME = 'Example';

export const PROTECTED = 'protected';
export const CLASSIFIED = 'classified';

const groups = {};
groups[RESOURCE_NAME] = [PROTECTED, CLASSIFIED];

export default groups;
