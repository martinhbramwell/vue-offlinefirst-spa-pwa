const UID = '    <user id>      ';
const PWD = '  <user password>  ';

const PRTL = 'http';
const HOST = '192.168.0.7';
const PORT = '9876';
const PATH = '  <where to go>  ';

const URL = `${PRTL}://${HOST}:${PORT}/${PATH}`;

export default {
  UID,
  PWD,
  PRTL,
  HOST,
  PORT,
  PATH,
  URL,
};
