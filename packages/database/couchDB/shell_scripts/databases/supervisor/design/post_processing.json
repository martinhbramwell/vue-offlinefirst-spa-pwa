{
  "_id": "_design/post_processing",
  "filters": {
    "by_request": "function(doc, req) { return doc._id === '_design/post_processing' || doc.meta && doc.meta.type && doc.meta.type == 'Request' }"
  }
}
